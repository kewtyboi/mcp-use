# Unified MCP (TS) — cloud-first example env

# --- Context7 Cloud (HTTP) ---
CONTEXT7_HTTP_URL=https://mcp.context7.com/mcp
# Optional API key (sent as header CONTEXT7_API_KEY)
# CONTEXT7_API_KEY=

# --- GitHub MCP (hosted, HTTP) ---
GITHUB_MCP_HTTP_URL=https://api.githubcopilot.com/mcp/

# OAuth (choose one source)
# 1) Provide a ready access token (skip the flow):
# GITHUB_MCP_OAUTH_TOKEN=<access_token>

# 2) Or let the unified server run the redirect flow and cache the token:
GITHUB_MCP_OAUTH_CLIENT_ID=<your_oauth_app_client_id>
# If your OAuth App requires a secret for code→token exchange, set it below:
GITHUB_MCP_OAUTH_CLIENT_SECRET=<your_oauth_app_client_secret>
# Scopes for OAuth App (adjust as needed)
GITHUB_MCP_OAUTH_SCOPES=repo,workflow,read:org,gist
# Must match your OAuth App settings
GITHUB_MCP_OAUTH_CALLBACK_URL=http://127.0.0.1:8789/callback
# Where the token is stored locally (git-ignored)
GITHUB_MCP_TOKEN_FILE=.tokens/github-mcp.json

# --- Unified server endpoints (for your IDE/host) ---
UNIFIED_WS_PORT=8777
UNIFIED_WS_HOST=127.0.0.1
