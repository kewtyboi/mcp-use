# Unified MCP (TS) – example env (cloud-first)

# --- Context7 Cloud (HTTP) ---
CONTEXT7_HTTP_URL=https://mcp.context7.com/mcp
# Optional API key for higher limits
# (Sent as header CONTEXT7_API_KEY by the unified server)
# If omitted, public rate limits apply
# CONTEXT7_API_KEY=

# --- GitHub MCP (cloud) – choose ONE transport ---
# If your GitHub MCP exposes HTTP:
# GITHUB_MCP_HTTP_URL=https://your-github-mcp.example/mcp
# If it exposes WebSocket:
# GITHUB_MCP_WS_URL=wss://your-github-mcp.example/mcp

# --- Unified server endpoints (for hosts) ---
UNIFIED_WS_PORT=8777
UNIFIED_WS_HOST=127.0.0.1
